<UserControl x:Class="Popup.Views.Contents.ImagePopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="760"
             d:DesignHeight="560">

    <!--
    IMAGE 팝업은 별도 제목/설명/크기 옵션 없이
    이미지 하나가 콘텐츠 영역 전체를 채운다.

    Stretch="UniformToFill"
    - 이미지 비율은 유지한다.
    - 빈 공간이 생기지 않도록 영역을 꽉 채운다.
    - 팝업과 이미지 비율이 다르면 가장자리 일부가 잘릴 수 있다.
    -->
    <Grid Background="Black">
        <Image x:Name="PopupImage"
               Stretch="UniformToFill"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch"
               MouseLeftButtonUp="PopupImage_MouseLeftButtonUp"/>
    </Grid>
</UserControl>
